<script type=javaScript/text>
// A simple calculator program by Adoga Sunday Owoicho
let result;

// Accept input from user
let operator = prompt('Enter an operator type ( either +, -, *, / ): ');

// take the operand input
var firstNumber = parseFloat(prompt('Enter first number: '));
var secondNumber = parseFloat(prompt('Enter second number: '));

switch(operator) {
    case '+':
         result = firstNumber + secondNumber;
        console.log('${firstNumber} + ${secondNumber} = ${result}');
        break;

    case '-':
         result = firstNumber - secondNumber;
        console.log('${firstNumber} - ${secondNumber} = ${result}');
        break;

    case '*':
         result = firstNumber * secondNumber;
        console.log('${firstNumber} * ${secondNumber} = ${result}');
        break;

    case '/':
         result = number1 / number2;
        console.log('${firstNumber} / ${secondNumber} = ${result}');
        break;

    default:
        console.log('You have entered and invalid operator');
        break;
}
</script>
